const {chromium}=require('C:/Users/parth/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://localhost:3000',{waitUntil:'networkidle'});await page.waitForTimeout(2200);await page.screenshot({path:'experience-desktop.png'});assert.equal(await page.locator('canvas').count(),1);assert.equal(await page.locator('.chapter').count(),6);await page.getByRole('button',{name:'Pause ambient animation'}).click();assert(await page.getByRole('button',{name:'Play ambient animation'}).isVisible());await page.locator('#capabilities').scrollIntoViewIfNeeded();await page.locator('summary').first().click();assert(await page.locator('details').first().getAttribute('open')!==null);await page.locator('.project-row').first().click();assert(await page.locator('dialog').isVisible());await page.keyboard.press('Escape');assert(!(await page.locator('dialog').isVisible()));await page.setViewportSize({width:390,height:844});await page.emulateMedia({reducedMotion:'reduce'});await page.goto('http://localhost:3000',{waitUntil:'networkidle'});await page.waitForTimeout(800);assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await page.screenshot({path:'experience-mobile.png'});await page.getByRole('button',{name:'Open navigation'}).click();await page.getByRole('navigation',{name:'Mobile navigation'}).getByRole('link',{name:'Why us',exact:true}).click();assert.equal(await page.locator('.mobile-nav').count(),0);const broken=await page.locator('a[href^="#"]').evaluateAll(a=>a.map(x=>x.hash).filter(x=>!document.getElementById(x.slice(1))));assert.deepEqual(broken,[]);assert.deepEqual(errors,[]);console.log('PASS: WebGL canvas, six chapters, animation toggle, service expansion, project dialog and Escape, mobile navigation, reduced motion, anchor targets, no mobile overflow, no runtime errors.');await browser.close();})().catch(e=>{console.error(e);process.exit(1)});
